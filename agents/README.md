# Agents

This directory contains OpenClaw-native definitions for QASecClaw:

- `workspaces/`: one workspace per agent (each with `workspace.json` and `SOUL.md`)
- `skills/`: tool adapter skills used by execution agents
- `schemas/`: shared evidence/report schemas (WIP)

